<article class="question">
  <div class="title">
    <a href="{{content}}" target="_blank">{{title}}</a>
    <span>({{content}}) </span>
  </div>
  <div class="meta">
 {{user.username}} on {{format_date created_at}}
  |
    <a href="/post/{{id}}">{{answers.length}} {{format_plural "answer" answers.length}}</a>
  </div>
</article>